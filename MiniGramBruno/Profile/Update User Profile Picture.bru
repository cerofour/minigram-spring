meta {
  name: Update User Profile Picture
  type: http
  seq: 2
}

post {
  url: {{base_url}}/user/me/profilePicture
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

body:multipart-form {
  file: @file(C:\Users\llacs\Documents\UTP\charly2.jpg) @contentType(image/jpeg)
}
